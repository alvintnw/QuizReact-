generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
}

model Pet {
  id          Int    @id @default(autoincrement())
  name        String
  species     String
  age         Int
  price       Int
  description String
}
